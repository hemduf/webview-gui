# Temporary #37 staged-validation exception.
#
# Pinned clap-validator 0.4.1 (152b9823e992d782c5c1fd33bca0295478b919aa)
# cannot correctly validate this Preset Discovery stage:
# - its FILE path parser requires every path to start with '/', which rejects
#   valid native Windows absolute paths such as C:/Users/...;
# - its metadata receiver deadlocks on the second begin_preset() call for a
#   multi-preset PLUGIN container, so both crawl and load time out.
#
# #91 remains blocking through .github/workflows/preset-contract.yml on all
# three desktop OSes. #95 MUST remove these exclusions after the validator is
# patched/upgraded and make native discovery+load validation blocking.
[test]
preset-discovery-crawl = false
preset-discovery-load = false
preset-discovery-descriptor-consistency = false
